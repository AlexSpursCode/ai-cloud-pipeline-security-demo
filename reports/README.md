# Reports

This directory contains both:

- pre-generated sample reports for presentation backup
- generated output under `reports/generated/`

The generated artifacts are intended to support the final security gate in CI.
